# TODO List

Список ниже является идеями для спецификаций.
Запускать каждый пункт списка задач в отдельной спецификации через команду `/magic.spec <description>` или `.agents\workflows\magic.spec.md <description>` с описанием задачи в поле `<description>`.
Когда пункт будет внесён в спецификацию, отметить его в этом списке как завершённый `[x]`.

## Tasks

- [ ] CLI Client for gonn
- [ ] можно во время обучения сети ставить паузу, продолжить работу
- [ ] делать через какой-то интервал снэпшоты состояния обучения сети, чтобы в случаи чего можно было вернуть, если что-то пошло не так, или если вдруг отрубят электричество/закончится заряд батареи ноута, пауза в обучении и т.д. хранить в json, переодически сжимать/архивировать старые снапшоты и удалять если они не нужны
- [ ] тесты и бенчмарки - покрыть ими весь функционал
- [ ] обязательно докстринги, комментарии и т.д. на английском языке (подробно всё разжовывать)
- [ ] предусмотреть механизм, чтобы можно было добавлять или удалять слои, нейроны во время обучения сети, работы сети и т.д.
- [ ] предусмотреть библиотеку так чтоб в будущем можно было визуализировать работу сети (показывать слои, нейроны, их состояние и т.д.) например в виде веб интерфейса или gui приложения, что через gui можно было управлять работой сети и т.д. (gui - это будет другой репозиторий)
- [ ] проработать исключения и ошибки и их обработку, очень важно, ошибки должны быть информативными!
- [ ] логирование - проработать логирование работы сети, чтобы можно было видеть, что происходит в процессе обучения и работы сети, как в про-проектах
- [ ] максимально использование стандартной библиотеки go, <https://cs.opensource.google/go>, минимизировать использование сторонних библиотек, только в крайнем случае и при необходимости
- [ ] поработать над тем, чтобы максимально ускорить работу сети, например с помощью векторизации, распараллеливания, использования GPU (OpenCL) и т.д.
- [ ] проработать вопрос с памятью, например чтобы сеть не загружала весь датасет в память, а работала с ним по частям, и т.д.
- [ ] в будущем в планах использовать GPU (например через CUDA или OpenCL), то стоит это учесть сразу на этапе проектирования.
- [ ] это наверное самая сложная механика, например: есть куча параметров НН, которые могут быть адаптивными или они должны подбираться под конкретные условия, под те или иные датасеты и т.д. например: ошибка, градиентный спуск, чтоб не пропускать его и не делать лишних просчётов, можно с помощью нашей ше сети обучать этот параметр рекурсия, надеюсь я понятно объяснил, если что спрашивай, надо предусмотреть такую дверь чтоб можно было любой параметр пропускать через нашу же сеть и обучать
- [ ] текущая архитектура проекта не конечная, может быть пересмотренна!
